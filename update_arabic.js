const fs = require('fs');
const path = require('path');
const file = path.join(__dirname, 'public', 'js', 'arabic.js');
let content = fs.readFileSync(file, 'utf8');

// Insert new keys at the end before the closing brace
const newTranslations = \
  // --- New Additions ---
  "order_tab_fetching": "?????",
  "order_tab_master": "????? ???????",
  "order_tab_deliveries": "?????????",
  "coll_prompt_manual_km": "???? ????? ??????????? ???????? ??????:",
  "coll_auto_km": "?? ??????",
  "coll_manual_km": "?? ????"
};
\;

content = content.replace(/\\s*\\}\\s*;\\s*$/, newTranslations);
fs.writeFileSync(file, content);
console.log("arabic.js updated!");
